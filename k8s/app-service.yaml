apiVersion: v1
kind: Service
metadata:
  name: ecommerce-app-service
  namespace: ecommerce

spec:
  selector:
    app: ecommerce-app

  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082

  type: NodePort